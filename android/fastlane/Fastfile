default_platform(:android)

platform :android do
  desc "Distribute a new build to app distribution"
  lane :distribute do
    sh "flutter clean"
    sh "flutter pub get"
    sh "flutter build apk --release"
    unless ENV['FIREBASE_TOKEN'] && !ENV['FIREBASE_TOKEN'].empty?
      UI.user_error!("Missing FIREBASE_TOKEN environment variable. Set it securely (e.g., CI secret) before running this lane.")
    end
    firebase_app_distribution(
      app: "1:10939381120:android:8582f62c850f58180ffd65",
      firebase_cli_token: ENV['FIREBASE_TOKEN'],
      android_artifact_type: "APK",
      android_artifact_path: "../build/app/outputs/flutter-apk/app-release.apk",
      testers: "omaralsalhani7@gmail.com,safyibrahim289@gmail.com",
      release_notes: "Fixed The Routing Problem and Some Localization Issues and added unit tests"
    )
  end
end